#include "main.h"
#include <string.h>

/**
 * _strcmp - function that compares two strings.
 * @s1: first pointer
 * @s2: second pointer
 *
 * Return: 0
 */

int _strcmp(char *s1, char *s2)
{
	strcmp(s1, s2);

	return (0);
}
