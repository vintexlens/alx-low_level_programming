#include "main.h"

/**
 * mul - function that multiplies two integers
 * @a: first varriable
 * @b: second varriable
 *
 * Return: 0
 */

int mul(int a, int b)
{
	int result;

	result = (a * b);

	return (result);
}
